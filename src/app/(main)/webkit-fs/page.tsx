import WebkitFSComponent from './_components/WebkitFSComponent';

export const metadata = {
  title: 'Webkit Presentation Mode POC | Creator Stage',
  description: 'Testing webkitSetPresentationMode for iOS Safari fullscreen with overlays.',
};

export default function WebkitFSPage() {
  return <WebkitFSComponent />;
}
